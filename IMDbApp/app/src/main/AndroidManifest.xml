<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.com.imdbapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/MainActivity_Label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SearchMovieActivity"
            android:label="@string/SearchMovieActivity_Label" />
        <activity android:name=".MovieDetailsActivity"
            android:label="@string/MovieDetailsActivity_Label"/>
        <activity android:name=".MovieWebViewActivity"
            android:label="@string/MovieWebViewActivity_Label"></activity>
    </application>

</manifest>
